apiVersion: serving.knative.dev/v1alpha1
kind: Service
metadata:
  name: logger3
spec:
  runLatest:
    configuration:
      revisionTemplate:
        spec:
          container:
            image: docker.io/justin2997/knative-rabbitmq-logger-pods:latest
            args:
              - '--routingKey=logger3.default'